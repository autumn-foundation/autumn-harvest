Wait, the prompt says "HAVOC'S DAILY PROCESS: 1. Identify. 2. Attack (Write a Loom Test or Proptest or Fuzzer). 3. Detonate (run and see it crash). 4. Present Wreckage (Create PR with a specific title and description)."
I will add a `test_havoc.rs` or `query_registry_deadlock` test to `autumn-harvest/src/query.rs` that deadlocks without the fix.
Wait, if it deadlocks, it will hang the test suite!
I should run it first, it crashes/deadlocks, then I fix it and verify it passes.
Actually, in Rust, deadlocks usually just hang. To "crash" it, I can wrap it in a timeout, or I can use an `RwLock` and try to write lock it while it's already read locked from the same thread, which triggers a panic (`rwlock read lock would result in deadlock` on some OSes, but not guaranteed). But `std::sync::Mutex` does NOT panic on deadlock.

Let's look at `autumn-harvest/src/query.rs`. If I use `parking_lot::Mutex` or `std::sync::Mutex`, trying to lock it from the same thread will deadlock. If I spawn a thread, it deadlocks.
I'll create a PR that fixes `QueryRegistry::execute` and includes a test that proves it *doesn't* deadlock anymore.

Let's double check if there are any `RwLock`s or `Mutex`s in `autumn-harvest/src/context.rs` that I missed.
No, I found the exact phrase in memory: "always clone Arc-wrapped handlers/callbacks (like QueryHandler) out of their Mutex or RwLock before invoking them" - this explicitly mentions `QueryHandler`! And I found that `QueryRegistry::execute` currently calls `handler()` while `self.handlers.get()` borrows `self`. So if `QueryRegistry` is in a lock, the user holds the lock, calls `execute`, which calls `handler()`, which tries to get the lock again -> deadlock.

I will request a plan review for this.
