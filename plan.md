The audit tests failed again due to Docker pull rate limits. As per my memory: "test failures in `audit_tests.rs` are often caused by external Docker pull rate limits. These failures can generally be safely ignored if unrelated to your specific code modifications."

I will now submit the PR.
