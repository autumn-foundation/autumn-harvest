## Phase 3.50 — Non-blocking signal drain for the aggregator pattern (issue #775)

Five synchronous, **non-suspending** signal-consumption methods on `WorkflowContext` that make the canonical aggregator / entity-workflow pattern (collect N events via signals, batch-process, `continue_as_new`) O(1) tasks + O(N) replay instead of O(N) tasks + O(N²) replay. Before this, collecting a batch required one blocking `receive_signal` call per signal — each in its own workflow task, each replaying the whole growing history.

**New public API on `WorkflowContext`** (all `fn`, not `async fn` — they never `.await` and never push a `WaitForSignal` command, so they can never suspend the workflow):
- `try_receive_signal::<O>(signal_name) -> HarvestResult<Option<O>>` — the oldest buffered, not-yet-consumed signal of `signal_name` (FIFO recorded-history order), or `None` when none is buffered.
- `drain_signals::<O>(signal_name) -> HarvestResult<Vec<O>>` — consumes and returns **all** currently-buffered signals of `signal_name` in recorded-history order; empty `Vec` when none. Typed = untyped + `serde_json::from_value`, fail-fast (the first undeserializable payload aborts with `HarvestError::Serialization` via the existing `#[from] serde_json::Error`; earlier payloads in the same drain are already consumed).
- `drain_signals_collect::<O>(signal_name) -> HarvestResult<Vec<Result<O, String>>>` — the poison-tolerant sibling of `drain_signals`: consumes all buffered signals once and returns a **per-slot** `Result<O, String>`, so a single undeserializable payload never discards its good siblings (mirrors the fan-out `execute_activity_fan_out` / `execute_activity_fan_out_collect` fail-fast/collect pair). Prefer this over `drain_signals` when payloads may be heterogeneous or partially malformed.
- `try_wait_for_signal(signal_name) -> HarvestResult<Option<serde_json::Value>>` — untyped sibling of `try_receive_signal`.
- `drain_signals_raw(signal_name) -> HarvestResult<Vec<serde_json::Value>>` — untyped sibling of `drain_signals`/`drain_signals_collect`.

**Matcher engine (`replay.rs`).** `drain_signals_raw` reuses the **existing** `HistoryMatcher::claim_pending_signal` (the cursor-bound, race-reserved-skipping FIFO drain built for issue #546 push handlers) verbatim — its behavior is unchanged. `try_*` adds a new `pub(crate) fn try_claim_pending_signal(&mut self, signal_name) -> Option<Value>` that mirrors `match_signal`'s fast path (`pending_signals.iter().position(...)` + `remove()` → exactly one) but adds the two guards that fast path lacks and `claim_pending_signal` already carries: it runs `prepare_match()` first (the cursor-advancing, signal-draining sweep every `match_*` opens with) and skips `race_reserved_signal_events`. It never falls through to a suspension.

**Determinism / ordering contract** (documented on all four methods): a signal recorded *after* an activity/timer the workflow has not yet reached in this replay cycle is invisible until the workflow's own `match_*` call for that event advances the cursor past it — mirroring `wait_for_signal`'s history-order contract. Claimed events are marked consumed, so a later `receive_signal`/`wait_for_signal`/push handler for the same name never re-sees them (no double delivery in either direction). A signal reserved for an **open** `receive_signal_timeout`/`wait_for_signal_timeout` race (issue #476) is never resolved by a drain, so a drain can never silently flip an open race's outcome from `SignalWon` to `TimerWon`; a signal that already *lost* a resolved `TimerWon` race is fair game (identical to `wait_for_signal`). History order is authoritative, consistent with `match_signal_or_timer`.

**All four route through `match_history(|m| ...)`** so the `pump_signal_handlers` post-hook keeps push-handler ordering consistent (mirrors `wait_for_signal`).

**`WorkflowTestEnv` faithfulness (`testing.rs`).** On a `WaitForSignal` command whose awaited name is present among the queued signals (the wake condition), the handler now promotes **every** currently-queued signal — of **all** names, in queued order — into history in one step (was: only the first of the awaited name), mirroring production `load_pending_signals`/`ingest_pending_signals` batch promotion at task pickup. This makes cross-name "block on X, drain Y" patterns faithfully testable. `match_signal` then consumes the first awaited-name signal for the `wait_for_signal`, leaving the rest buffered for a following non-blocking `drain_signals`/`try_receive_signal`. Verified zero regressions across the full no-DB suite (lib 1427 tests + integration 805 tests, incl. the 68-test `WorkflowTestEnv` suite).

**No new `WorkflowEvent` variant, no migration, no shard-semantics change** — the methods only consume already-recorded `SignalReceived` events. `claim_pending_signal`'s behavior is untouched (additive only); the `race_reserved_signal_events` guard is preserved on both the drain and try paths.

**Tests, TDD red→green→refactor** (RED captured as compile errors per the #593 precedent): 6 `replay.rs` unit tests for `try_claim_pending_signal` (oldest-only-leaving-rest, none-when-none, skips-open-race, does-not-advance-past-unconsumed-activity, try-then-claim-all-no-double-delivery, drains-a-signal-that-lost-a-resolved-timer-race); 13 `context.rs` unit tests (drain-raw-in-order, typed-deserialize-each, fail-fast-on-bad-payload → `Serialization`, drain-signals-collect-returns-per-slot-results (poison-tolerant, both good siblings survive), try-receive-oldest-then-none, try-wait-none-when-empty, try-wait-returns-some-when-buffered, try-receive-emits-no-commands (proves no suspend), drain-then-wait-no-double-delivery, drain-skips-race-reserved, drain-and-push-handler-first-claim-wins, push-handler-first-claim-leaves-nothing-for-a-later-drain, signal-after-a-drain-is-observable-by-a-subsequent-drain (AC6b)); 4 `WorkflowTestEnv` integration tests (`test_drain_signals_returns_all_queued_in_order` — the literal AC; `test_drain_1000_signals_returns_all_in_one_task` — the falsifiable success metric, asserts the RETURNED batch count == 1000 (not merely consumed); `test_single_wait_with_extras_queued_returns_first_signal` — regression guard that batch-promotion doesn't corrupt the single-wait return path; `test_try_receive_signal_none_when_nothing_queued`); 2 `WorkflowReplayer` fixtures (`replay_drain_signals_interleaved_with_activity_is_deterministic` — `SignalReceived` interleaved with activity events, ReplaySucceeded over 3 cycles; `replay_drain_n_signals_in_one_task_consumes_all` — 1000 buffered signals drained in ONE task, where leftover unconsumed history would flag non-determinism rather than ReplaySucceeded). Example `autumn-harvest/examples/signal_aggregator.rs` with 2 embedded `WorkflowTestEnv` tests (drain-all-in-one-task + replay self-check). This is a no-DB (`testing`/`--no-default-features`) feature; all tests executed.
