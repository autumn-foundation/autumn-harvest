# Assay ledger

Records of R&D spikes run against a falsifiable question, a named decider, and
numeric success/kill lines committed *before* the first measurement. See each
entry for its pre-registration commit, apparatus, and verdict.

| # | question | verdict | report |
|--:|:--|:--|:--|
| 1 | Does the built `autumn-harvest-redis` adapter clear its own spec's >10,000 ops/sec bar, on a machine where nothing else can slow it down? | **kill**, narrowly: an 8-worker/steady-state sub-question this assay added on its own (8,760 mean vs 10,000 ops/sec). The founding spec's actual, unconstrained claim looks achievable, not refuted — an exploratory backlog-drain check (post-hoc, not pre-registered, and not a verified match for the Postgres control's workload shape after two attempts) hit 12,004 mean on its own. No multiplier against Postgres is reported. Unintegrated with the worker regardless of either number. | [0001-redis-adapter-throughput-ceiling.md](0001-redis-adapter-throughput-ceiling.md) |
| 2 | Re-charter of #1: at `docs/performance.md`'s own matched scenario shape (8 claimers, 4 queues, bounded-fraction claim-only draw, ported by value from `claim_bench_support.rs`), does `RedisTaskQueue::claim` clear a decisive (10x) margin over Postgres's published claims/s at the 10,000-row headline cell? | **pursue** — mean 18,933.43 claims/s across 4 runs (range 18,334.91-19,845.62) vs. a 290 claims/s (10x) line, against a published Postgres control of 29 claims/s (~653x on the mean, ~63x on the worst individual run). `n` matched the published Postgres `n` exactly at both the 1,000- and 10,000-row cells; post-review (Codex) caught the first apparatus silently reading only one of the four queues, fixed by rotating queue order per call and directly verified via `XPENDING` (200 claimed per queue). Still unintegrated with the worker; a deployment-shaped follow-up remains a shelf entry until the integration refactor exists. | [0002-redis-matched-workload-vs-postgres.md](0002-redis-matched-workload-vs-postgres.md) |
| 3 | `docs/performance.md`'s own open question: does replacing the concurrency-key claim gate's correlated subquery against an unindexed `MATERIALIZED` CTE with one against `harvest_task_queue` itself, backed by a new partial index, keep the idle-case cost cheap *and* fix the documented 5,000-key hot-contention blowup (1,600ms) *and* not regress the 256-key hot-contention case? | **kill**, on the idle line: candidate's idle-case (10,000-row backlog, 256 or 5,000 keys, zero RUNNING rows) cost ~10,130 buffers vs. a ≤50-buffer line — 200x+ over, and identical regardless of key cardinality. Neither variant gets an `ORDER BY … LIMIT` pushdown at this backlog depth (control scans the same 10,000 rows); the gap is ~10,000 real per-row index probes vs. ~10,000 near-free probes of a small, resident, empty CTE — a gap that holds (and widens in relative terms) under a post-review finding that the line's own source figure was subplan-scoped, not whole-query. The high-cardinality fix itself works and works well (24.2ms vs. a 160ms line, ~48.8x faster than same-run control) and the 256-key case doesn't regress either (47.0ms, faster than its own control) — but one miss among three pre-set lines is a kill by the pre-registered rule. This specific formulation is closed; other shapes (a `LATERAL`-with-hints variant, or deferring the expensive gate to the already-cheap `claimed` CTE recheck) remain untested, un-re-chartered pits. | [0003-concurrency-gate-cardinality-index.md](0003-concurrency-gate-cardinality-index.md) |
| 4 | Re-charter of #3's second named pit (its first, a `LATERAL`+hints variant, was already closed by `docs/performance.md`'s own prior analysis): does removing the concurrency-key predicate from candidate selection entirely and enforcing the cap only in the `claimed` CTE's existing authoritative recheck — retrying against the next candidate on a failed recheck — keep idle cost at parity, fix the 5,000-key blowup, not regress the 256-key case, *and* keep retry count bounded when the highest-priority rows are themselves blocked by a saturated key? | **kill**, on the new (4th) line this shape itself requires: idle parity (132 vs. 132 buffers, tie), the 5,000-key blowup (6.8ms vs. a 160ms line, 218.5x faster than same-run control), and the 256-key case (6.9ms, 30.4x faster than control) all pass decisively — but a 50-row adversarial fixture (highest-priority PENDING rows keyed to already-saturated concurrency keys) costs 313.8ms against a ≤100ms line, 3.14x over, because each retry re-runs the full candidate-selection scan and nothing bounds how many consecutive high-priority rows can share a saturated key. A different failure mode than #3's: bounded-but-wrong there, unbounded-and-workload-dependent here. A capped-retry-with-fallback variant remains an untested, un-re-chartered pit. | [0004-concurrency-gate-deferred-recheck.md](0004-concurrency-gate-deferred-recheck.md) |
| 5 | Issue #1340's own named-but-untried fix for #1177's general "any residual predicate defeats sort-elision" defect: does a **batched** seek-and-refine rewrite — one query fetches the top 50 ordered candidates, a concurrency-recheck CTE scoped only to that batch's own distinct keys (not the backlog's global cardinality, unlike #3), first eligible row wins in the same round trip — clear idle parity, the 5,000-key blowup *cardinality-independently*, the 256-key case, ledger #4's own 50-row adversarial fixture in one round trip, *and* a 4x-deeper fixture in proportionally more round trips? | **kill**, on a pre-registration arithmetic error, not a mechanism finding: idle (181 vs. ≤300 buffers), the 5,000-key case (8.2ms vs. a 160ms line, 179.7x faster than same-run control, confirming the batch-scoped CTE's cost is independent of global key cardinality — the property #3's global index lacked), the 256-key case (8.6ms, 25.8x faster than control), and both adversarial fixtures' wall-clock (14.7ms and 33.9ms, 6.8x and 11.8x inside their lines) all pass decisively — but both adversarial fixtures resolved in one more batch than the pre-registered "exactly N" line required (2 vs. 1; 5 vs. 4), because the line's own formula undercounted by the one slot the claimable row itself occupies (`ceil((poison_depth+1)/batch_size)`, not `ceil(poison_depth/batch_size)`). One miss is a kill by the pre-registered rule regardless of why the line was wrong; the same data shows the batching mechanism itself scaling linearly in batch count exactly as designed, and a corrected re-charter remains an open, explicitly-not-yet-run pit — as does the concurrent-claimer lock-contention risk this and every prior concurrency-gate assay has left unmeasured. | [0005-claim-batched-seek-and-refine.md](0005-claim-batched-seek-and-refine.md) |
