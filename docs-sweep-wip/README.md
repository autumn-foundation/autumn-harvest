# docs-sweep-wip/ — TEMPORARY resilience copy (DELETE BEFORE PR)

This directory holds the PLANNING-phase artifacts for the 0.5.0 documentation sweep
(`plan.md`, `partition.md`). It exists ONLY so a session reset does not lose the plan.

**It MUST be removed (`git rm -r docs-sweep-wip/`) before the final docs PR is opened.**

Canonical copies also live in the session scratchpad. See `plan.md` §0 for environment
facts and the deferred-pin / hard-exclusion guardrails.
