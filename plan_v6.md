1. Use `replace_with_git_merge_diff` to update the path syntax to use `{id}` instead of `:id` but we must add `without_v07_checks` or just not mock it that way. Actually, the axum 0.7 router uses `{id}` for variables. Wait! The panic says: "Path segments must not start with `:`. For capture groups, use `{capture}`. If you meant to literally match a segment starting with a colon, call `without_v07_checks` on the router."
Wait, my previous diff CHANGED `{id}` to `:id`. I need to change it back to `{id}` and `{name}` because Axum 0.7 uses `{id}`. Let's change it to `{id}` and `{name}`. But wait! When I had `{id}` previously, the test failed? No, when I had `{id}` previously, the review failed. Let's check the code review. The code review said: "Axum does not use `{id}` for path parameters; it uses the `:id` syntax (e.g., `"/workflows/:id/query/:name"`)." But Axum 0.7 changed the syntax to `{id}`! The code reviewer was wrong. I will change it back to `{id}`.
2. Update the router to use `"/workflows/{id}/query/{query_name}"` since the handler uses `query_name`.
3. Submit PR.
