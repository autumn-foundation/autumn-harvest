[package]
name = "quickstart"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
autumn-harvest = { version = "0.3.0", path = "../../autumn-harvest", features = ["db"] }
autumn-harvest-plugin = { version = "0.3.0", path = "../../autumn-harvest-plugin" }
autumn-web = "0.4"
serde_json = { workspace = true }
tracing = { workspace = true }
